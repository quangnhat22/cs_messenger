class AppPrefKey {
  AppPrefKey._();

  static const String token = 'token';
  static const String refreshToken = 'refreshToken';
}

class AppBoxKey {
  AppBoxKey._();

  static const String isFirstInstalled = 'isFirstInstalled';
}

class AppNetworkKey {
  AppNetworkKey._();

  static const authorization = 'Authorization';
  static const bearer = 'Bearer';
}
